"""RG2 그리퍼 동작 파라미터(파지력·대기·측정·폐기물)."""
# 자동 분리: grip_config.py → config/ (내용 동일, 위치만 이동)

GRIPPER = {
    'open_force': 200,     # 개방 힘 20N ×10
    'open_width': 1100,    # 최대 개방 110mm ×10
    'grip_force': 400,     # 파지 힘 40N ×10 (판정·이송 내내 유지)
    'contact_force': 5.0,  # 접촉 감지 힘(N) — 1차 탐지 및 테니스 확정 측정
    # ★야구공 2차 접촉힘(N). 야구(145g)는 테니스(58g)의 2.5배라 5N으로는 공을 그리퍼
    #   가운데로 밀어내지 못해 접촉이 빗나간다(실측: w5가 70~73 대신 75~77로 부풀고
    #   그 오차가 눌림까지 부풀려 하드를 소프트로 오판). 1차에서 야구로 판명되면
    #   완전히 열고 이 힘으로 '다시 접근'해 정렬시킨다(힘만 올리면 이미 잘못 물린 상태라 무의미).
    #   10N인 이유: 눌림 신호 손실이 5→10N 구간은 6%뿐(12N은 20%, 20N은 66%).
    'contact_force_baseball': 10.0,
    # ── 통 무게 측정 ─────────────────────────────────────────────────
    # 노이즈 대책: ①절사평균(이상치 제거) ②촘촘한 샘플링 ③'실제로 잠잠해진 뒤' 측정
    'fz_sample_s':   0.1,   # 샘플 간격(s) — 0.25는 표본이 6~8개뿐이라 이상치에 취약했다
    'fz_window_s':   1.5,   # 안정 후 평균낼 구간(s)
    'fz_trim':       0.2,   # 상·하위 20%씩 잘라내는 절사평균 비율
    'fz_settle_std': 0.35,  # 이 표준편차(N) 이하가 되면 '진동 잦아듦'으로 판단
    'fz_settle_n':   8,     # 안정 판정에 쓰는 최근 표본 수
    'fz_settle_max_s': 6.0, # 안정 대기 상한(s) — 넘으면 그대로 진행하고 경고
    # ★영점(fz0)을 '그리퍼 완전 개방 = 무부하' 상태에서 잡는다.
    # 잠근 뒤 그대로 재면 뚜껑 하중을 통이 받는지 그리퍼가 받는지 애매해 Δ에 편차가 생긴다.
    # 개방 상태면 로봇에 걸린 외력이 확정적으로 0 → Δ = 뚜껑+통+내용물 (재현성 높음).
    'weight_tare_open': True,
    'weight_regrip_force': 10.0,   # 영점 측정 후 뚜껑 재파지 힘(N)
    'waste_grip_force': 5.5,   # 폐기통 파지력(N)
    'waste_grip_wait': 2.0,    # (미사용) 예전 고정 대기 — 지금은 물림 확인 방식
    'waste_grip_timeout': 6.0, # 파지 성립 대기 상한(s)
    'waste_grip_min_w': 15.0,  # 물린 폭 하한(mm) — 이보다 작으면 헛잡음
    'waste_grip_max_w': 105.0, # 물린 폭 상한(mm)
    'waste_grip_retry': 1,     # 파지 실패 시 개방 후 재시도 횟수
    'waste_open_force': 20.0,  # 폐기통 놓을 때 개방힘(N)
    # 순응 하강량(mm). None이면 티칭된 T10 좌표를 그대로 쓰고, 값을 주면
    # P12 기준으로 그만큼만 내려간다(회전 잠금도 그 높이에서 수행).
    'lid_descend_mm': 26.8,
    'lid_release_force': 15.0,  # 뚜껑/통을 놓을 때 벌리는 힘(N) — 너무 세면 놓는 순간 충격
    'align_force': 40.0,   # 야구 정렬용 '꽉 잡기' 힘(N) — 5N으로 못 밀던 공을 강제로 중앙 정렬
    'align_hold': 0.8,     # 꽉 잡은 채 유지(s) — 공이 중앙으로 밀려 자리잡는 시간
    'align_recover': 0.5,  # 개방 후 재접근 전 대기(s) — 눌렸던 공이 형상 회복할 시간(점탄성)
    'measure_retry': 2,    # 접촉 감지 실패 시 자동 재시도 횟수(완전 개방 후 다시 접근)
    # 새 닫기 명령 후 이만큼 이상 폭이 줄거나 busy가 관측돼야 grip FLAG를 이번 접촉으로 인정.
    # 개방 직후 남아 있는 이전 gSTA bit1을 새 공 접촉으로 오인하는 것을 막는다.
    'contact_fresh_travel': 2.0,  # 접촉 인정 최소 이동량(mm) — 0.5는 노이즈 범위라 잔류 플래그 통과
    'relax_force':   5.0,  # 판정 전 힘 빼기(40N crush 해제, 공은 계속 물고 있음)
    'open_wait': 1.6,      # 개방 완료 대기(s)
    'grip_wait': 1.2,      # 40N 파지 안정 대기(s)
    'measure_wait': 1.5,   # 판정 시 40N 재확인 대기(s)
    'release_wait': 1.0,   # 놓기 대기(s)
    # 접촉폭이 이보다 작으면 '물체 없음(미검출)'. 2026-07 그리퍼 교체로 파지력이 커져
    # 빈 손으로 닫을 때 더 좁게 닫히므로, 여유를 두어 미검출을 확실히 잡는다.
    # (실공 최소가 63mm대라 이 값을 올려도 정상 파지를 미검출로 오판할 위험은 없다.)
    'min_ball_width': 15.0,
    'min_hold_width': 30.0,  # 40N폭이 이보다 작으면 '놓침'(공이 빠져 거의 다 닫힘) — 불량 오판 방지
}

# ── 웨이포인트 역할 (role 필드 우선, 없으면 이름 키워드로 fallback) ───────────
# 역할: pick=잡는위치, via=경유지(진입 전), via2=집은후경유, pack=포장, defect=불량, home=홈(선택)
